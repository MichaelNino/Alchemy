project('alchemy-framework', 'c',
  version: '0.1.0',
  meson_version: '>= 0.62.0',
)

gnome = import('gnome')

gjs_dep = dependency('gjs-1.0')
gtk4_dep = dependency('gtk4', version: '>= 4.0.0')

app_id = 'org.alchemy.Demo'

# Subdirectories for data and src if we want to install
# subdir('data')
# subdir('src')

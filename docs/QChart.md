# QChart

## Overview
`QChart` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QChart } from 'alchemy';

const component = new QChart({
    // props
});
```

## Advanced
Look at `src/components/QChart.js` for exact prop definitions and GJS implementations.

# QOptionGroup

## Overview
`QOptionGroup` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QOptionGroup } from 'alchemy';

const component = new QOptionGroup({
    // props
});
```

## Advanced
Look at `src/components/QOptionGroup.js` for exact prop definitions and GJS implementations.

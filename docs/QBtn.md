# QBtn

## Overview
`QBtn` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QBtn } from 'alchemy';

const component = new QBtn({
    // props
});
```

## Advanced
Look at `src/components/QBtn.js` for exact prop definitions and GJS implementations.

# QTable

## Overview
`QTable` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QTable } from 'alchemy';

const component = new QTable({
    // props
});
```

## Advanced
Look at `src/components/QTable.js` for exact prop definitions and GJS implementations.

# QToolbar

## Overview
`QToolbar` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QToolbar } from 'alchemy';

const component = new QToolbar({
    // props
});
```

## Advanced
Look at `src/components/QToolbar.js` for exact prop definitions and GJS implementations.

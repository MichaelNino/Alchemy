# QForm

## Overview
`QForm` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QForm } from 'alchemy';

const component = new QForm({
    // props
});
```

## Advanced
Look at `src/components/QForm.js` for exact prop definitions and GJS implementations.

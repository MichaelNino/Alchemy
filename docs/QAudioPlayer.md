# QAudioPlayer

## Overview
`QAudioPlayer` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QAudioPlayer } from 'alchemy';

const component = new QAudioPlayer({
    // props
});
```

## Advanced
Look at `src/components/QAudioPlayer.js` for exact prop definitions and GJS implementations.

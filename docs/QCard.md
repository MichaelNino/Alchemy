# QCard

## Overview
`QCard` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QCard } from 'alchemy';

const component = new QCard({
    // props
});
```

## Advanced
Look at `src/components/QCard.js` for exact prop definitions and GJS implementations.

# QCodeViewer

## Overview
`QCodeViewer` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QCodeViewer } from 'alchemy';

const component = new QCodeViewer({
    // props
});
```

## Advanced
Look at `src/components/QCodeViewer.js` for exact prop definitions and GJS implementations.

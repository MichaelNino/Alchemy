# QToggle

## Overview
`QToggle` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QToggle } from 'alchemy';

const component = new QToggle({
    // props
});
```

## Advanced
Look at `src/components/QToggle.js` for exact prop definitions and GJS implementations.

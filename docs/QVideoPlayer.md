# QVideoPlayer

## Overview
`QVideoPlayer` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QVideoPlayer } from 'alchemy';

const component = new QVideoPlayer({
    // props
});
```

## Advanced
Look at `src/components/QVideoPlayer.js` for exact prop definitions and GJS implementations.

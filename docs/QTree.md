# QTree

## Overview
`QTree` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QTree } from 'alchemy';

const component = new QTree({
    // props
});
```

## Advanced
Look at `src/components/QTree.js` for exact prop definitions and GJS implementations.

# QTabs

## Overview
`QTabs` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QTabs } from 'alchemy';

const component = new QTabs({
    // props
});
```

## Advanced
Look at `src/components/QTabs.js` for exact prop definitions and GJS implementations.

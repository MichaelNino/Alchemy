# QList

## Overview
`QList` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QList } from 'alchemy';

const component = new QList({
    // props
});
```

## Advanced
Look at `src/components/QList.js` for exact prop definitions and GJS implementations.

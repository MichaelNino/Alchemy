# QIcon

## Overview
`QIcon` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QIcon } from 'alchemy';

const component = new QIcon({
    // props
});
```

## Advanced
Look at `src/components/QIcon.js` for exact prop definitions and GJS implementations.

# QSlider

## Overview
`QSlider` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QSlider } from 'alchemy';

const component = new QSlider({
    // props
});
```

## Advanced
Look at `src/components/QSlider.js` for exact prop definitions and GJS implementations.

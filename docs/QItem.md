# QItem

## Overview
`QItem` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QItem } from 'alchemy';

const component = new QItem({
    // props
});
```

## Advanced
Look at `src/components/QItem.js` for exact prop definitions and GJS implementations.

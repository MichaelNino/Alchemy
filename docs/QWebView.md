# QWebView

## Overview
`QWebView` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QWebView } from 'alchemy';

const component = new QWebView({
    // props
});
```

## Advanced
Look at `src/components/QWebView.js` for exact prop definitions and GJS implementations.

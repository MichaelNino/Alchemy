# QSpinner

## Overview
`QSpinner` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QSpinner } from 'alchemy';

const component = new QSpinner({
    // props
});
```

## Advanced
Look at `src/components/QSpinner.js` for exact prop definitions and GJS implementations.

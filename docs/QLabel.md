# QLabel

## Overview
`QLabel` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QLabel } from 'alchemy';

const component = new QLabel({
    // props
});
```

## Advanced
Look at `src/components/QLabel.js` for exact prop definitions and GJS implementations.

# QProgressBar

## Overview
`QProgressBar` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QProgressBar } from 'alchemy';

const component = new QProgressBar({
    // props
});
```

## Advanced
Look at `src/components/QProgressBar.js` for exact prop definitions and GJS implementations.

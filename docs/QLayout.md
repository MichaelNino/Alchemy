# QLayout

## Overview
`QLayout` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QLayout } from 'alchemy';

const component = new QLayout({
    // props
});
```

## Advanced
Look at `src/components/QLayout.js` for exact prop definitions and GJS implementations.

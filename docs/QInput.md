# QInput

## Overview
`QInput` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QInput } from 'alchemy';

const component = new QInput({
    // props
});
```

## Advanced
Look at `src/components/QInput.js` for exact prop definitions and GJS implementations.

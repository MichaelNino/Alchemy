# QFile

## Overview
`QFile` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QFile } from 'alchemy';

const component = new QFile({
    // props
});
```

## Advanced
Look at `src/components/QFile.js` for exact prop definitions and GJS implementations.

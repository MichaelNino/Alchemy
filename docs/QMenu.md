# QMenu

## Overview
`QMenu` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QMenu } from 'alchemy';

const component = new QMenu({
    // props
});
```

## Advanced
Look at `src/components/QMenu.js` for exact prop definitions and GJS implementations.

# QCardSection

## Overview
`QCardSection` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QCardSection } from 'alchemy';

const component = new QCardSection({
    // props
});
```

## Advanced
Look at `src/components/QCardSection.js` for exact prop definitions and GJS implementations.

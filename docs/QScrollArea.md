# QScrollArea

## Overview
`QScrollArea` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QScrollArea } from 'alchemy';

const component = new QScrollArea({
    // props
});
```

## Advanced
Look at `src/components/QScrollArea.js` for exact prop definitions and GJS implementations.

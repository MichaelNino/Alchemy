# QDialog

## Overview
`QDialog` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QDialog } from 'alchemy';

const component = new QDialog({
    // props
});
```

## Advanced
Look at `src/components/QDialog.js` for exact prop definitions and GJS implementations.

# QGrid

## Overview
`QGrid` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QGrid } from 'alchemy';

const component = new QGrid({
    // props
});
```

## Advanced
Look at `src/components/QGrid.js` for exact prop definitions and GJS implementations.

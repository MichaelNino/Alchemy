# QNotify

## Overview
`QNotify` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QNotify } from 'alchemy';

const component = new QNotify({
    // props
});
```

## Advanced
Look at `src/components/QNotify.js` for exact prop definitions and GJS implementations.

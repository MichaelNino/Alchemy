# QCheckbox

## Overview
`QCheckbox` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QCheckbox } from 'alchemy';

const component = new QCheckbox({
    // props
});
```

## Advanced
Look at `src/components/QCheckbox.js` for exact prop definitions and GJS implementations.

# QSplitter

## Overview
`QSplitter` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QSplitter } from 'alchemy';

const component = new QSplitter({
    // props
});
```

## Advanced
Look at `src/components/QSplitter.js` for exact prop definitions and GJS implementations.

# QSelect

## Overview
`QSelect` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QSelect } from 'alchemy';

const component = new QSelect({
    // props
});
```

## Advanced
Look at `src/components/QSelect.js` for exact prop definitions and GJS implementations.

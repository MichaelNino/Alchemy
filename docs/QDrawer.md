# QDrawer

## Overview
`QDrawer` is a native UI component in the Alchemy Framework.

## Usage
```javascript
import { QDrawer } from 'alchemy';

const component = new QDrawer({
    // props
});
```

## Advanced
Look at `src/components/QDrawer.js` for exact prop definitions and GJS implementations.
